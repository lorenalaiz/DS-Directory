# Base URL used by UI tests
BASE_URL=

# Add any auth tokens / secrets needed for tests below.
# Never commit the real .env file — only .env.example.

# Throwaway admin account in the Firebase Auth emulator, used by admin-flow
# Playwright tests. Must point at the emulator only — never a real account.
ADMIN_TEST_EMAIL=
ADMIN_TEST_PASSWORD=
